# KCI 빅데이터/AI 연구 대시보드 - 프로젝트 개선 방안

이 문서는 프로젝트의 향후 유지보수 및 기능 고도화를 위한 핵심 개선 목표를 정리한 것입니다.

## 1. 파일의 분할 (코드 모듈화)
**목표:** 현재 `index.html` 하나에 집중된 코드를 분할하여 코드 가독성을 높이고 수정 작업을 편리하게 만듭니다.
*   **HTML (`index.html`)**: 웹 대시보드의 기본 뼈대와 레이아웃 구조만 담당합니다.
*   **CSS (`style.css`)**: 다크 모드, 글래스모피즘 등 디자인 및 애니메이션 코드를 전담합니다.
*   **JavaScript (`script.js`)**: Chart.js를 이용한 그래프 렌더링 및 동적 로직을 분리하여 관리합니다.

## 2. 하드코딩에서 벗어나기 (동적 데이터 연동)
**목표:** 코드 내에 직접 적혀있는(하드코딩된) 데이터 대신, 실제 엑셀 파일(`.xls`) 데이터를 자동으로 읽어오도록 구성합니다.
*   **1안 (프론트엔드 방식):** 파이썬이 없는 환경에서도 작동하도록 브라우저 자체(자바스크립트 라이브러리 활용)에서 엑셀 파일을 바로 읽고 차트를 업데이트하는 방안입니다.
*   **2안 (파이썬 연동 방식):** 파이썬이 설치된 환경에서 파이썬(Pandas 등)을 이용해 엑셀 데이터를 즉시 분석하고, 그 결과를 웹 대시보드에 띄워주는 방안입니다.

## 3. 단어 선별 (인문학적 관점 강화)
**목표:** '인공지능'과 '빅데이터' 두 핵심 단어와 연관된 키워드를 분석할 때, 과학기술 전문 용어가 너무 많이 도출되는 현상을 개선합니다.
*   **개선 방향:** 순수 과학/기술 단어들을 필터링(제외 처리)하고, 윤리, 저작권, 사회적 영향 등 **인문학적/철학적 관점의 단어**들과의 연관성이 더 잘 드러나도록 데이터 정제(단어 선별) 과정을 새롭게 추가합니다.
